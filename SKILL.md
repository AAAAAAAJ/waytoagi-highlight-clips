---
name: waytoagi-highlight-clips
description: 将飞书妙记、共学直播或本地长视频制作成适合二次传播的高光短视频，支持案例追加、去口癖与冗余片段精剪、前3秒优化、同步字幕、WaytoAGI品牌排版和飞书交付。用户要实际成片或沿用这套剪辑风格时使用。
---

# WaytoAGI 高光剪辑

将有来源的长视频整理成独立、能看懂、能兑现开头看点的短视频。默认沿用灰白底、黑色大字、彩色点缀和原版 Logo 黑色页眉。用户指定其他品牌或版式时按其要求调整。

## 确定交付

- 从会话判断用户要筛选清单、样片、完整成片或改版。已要求成片时，完成视频导出和可用文件交付。
- 复用已有视频、剪辑清单与字幕；追加案例保留编号，样式改版覆盖用户指定范围。
- 未指定平台时采用 1080×1920、25 fps、H.264/AAC 通用竖版；条数和时长由内容完整性决定。多数独立案例约 25–75 秒，复杂演示可适当加长。
- 源视频、时间码、原话和作者信息保持可追溯。原录制日期与本次制作日期分别填写。

## 获取与筛选

优先使用用户提供的本地视频和字幕。只有飞书链接时，使用可用飞书工具或 CLI 读取妙记、逐字稿和授权可用的录像；接口操作按已安装的 `lark-minutes`、`lark-doc` 或 `lark-drive` 技能读取。登录、访问权限或原视频确实缺失时，说明具体缺口并请求所需素材，同时完成可独立进行的文本筛选。

读 [editorial-workflow.md](references/editorial-workflow.md)，从实际演示、鲜明观点、可操作方法或有趣原话中选独立主题。记录来源属性：本人实测、现场试玩、第三方案例解读、创作段子等；写入画面和交付说明。保留影响判断的条件和限制。

## 先设计前 3 秒

每条先写清：**观众会看到什么、为什么值得继续看、后文在哪里兑现**。按 [opening-hooks.md](references/opening-hooks.md) 选择结果先行、具体问题、原话趣点或操作前后变化。

- 第一帧就有具体画面与短标题，品牌署名融入版式。
- 优先用同次演示的真实结果画面配原声；需要原话前置时，保留完整语义并同步重排声音、画面和字幕。
- 开头编辑标题与逐字字幕分开。标题可以概括，字幕跟随实际说话内容。
- 前 3 秒让人愿意继续看；后文及时展示步骤、变化或结论。用真实留存数据检验，避免承诺固定完播提升。

## 口癖与冗余片段精剪

高光制作默认做一轮自然精剪；用户要求“去口癖、删废话、紧凑一点”时重点应用 [speech-cleanup.md](references/speech-cleanup.md)。先删除无关铺垫与同义复述，再清理无意义填充音、重复起句和冗长停顿。结合语义判断“然后、其实、就是”等词，保留否定、数量、必要条件、强调语气与关键演示音效。

按原音复核删除区间，写出删除理由和前后保留词；通过 `scripts/refine_speech.py` 生成新项目，实际修改保留区间并同步调整字幕、补充画面、来源标签、封面和开头标题时间。仅清理字幕不能代表原声精剪完成。新项目另存，渲染后检查新剪口与真实缩短时长；无法确认词尾时保留原音。

## 校对与剪辑

对照逐字稿和原音确定入出点；词级 ASR 用于对齐和发现疑点，专名以原话和可见界面共同核对。剪点落在帧网格，保留词尾和必要呼吸。字幕映射到最终拼接时间线。

屏幕录制可能晚于解说：检查画面是否已出现对应按钮、动作和结果。必要时用同次演示的补充画面或结果静帧，保持原声，在剪辑清单记录两条时间线。静帧写明源帧点；多个网上作品按画面切换署名。

## 品牌与渲染

首次使用视觉模板或改样式时读 [brand-style.md](references/brand-style.md)。`assets/brand-logo-white.png` 为原版组合标识；保留完整透明边距、图文比例和配色，等比缩放。中文字体从本机选择，系统字体不打包传播。

按 [project-schema.md](references/project-schema.md) 准备项目 JSON，可从 `assets/project.example.json` 起步。路径、日期、讲者、标题、编号、字幕与补充画面均由项目提供。

```bash
python3 <skill-dir>/scripts/render_clips.py --project <project.json> --validate-only
python3 <skill-dir>/scripts/render_clips.py --project <project.json> --clip 01 --sample 8
python3 <skill-dir>/scripts/render_clips.py --project <project.json>
```

依赖 Python、Pillow、FFmpeg 和 ffprobe。首遍测量各条原声，再做响度处理；声音、画面、字幕共用剪辑区间。样片与正式文件分开；修改过的内容重新生成和验证。

## 验收与交付

运行 `scripts/verify_media.py --help`，检查实际分辨率、帧率、编码、音画时长差、完整解码和峰值。技术通过后检查第一帧、前 3 秒、剪切处、补画面边界、署名切换与最后一帧；标题承诺的结果须真实出现。静音看开头也应知道主题。

用 `scripts/package_delivery.py --project <project.json>` 生成发布文案、时间码和素材包。MP4、JPG、SRT 使用相同编号；包清单与当前文件一致。日期、条数、时长与大小从产物计算。

按用户的交付渠道执行。本工作区默认飞书交付，按 [feishu-delivery.md](references/feishu-delivery.md) 创建或更新文档，提供单条预览和整包下载。已获授权的交付继续完成；制作成片不等于授权社交账号发帖。

只替换本次改变的素材；保留项目 JSON 和文件校验值，便于追加案例和改版。
